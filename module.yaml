name: forge-reflect
version: 0.2.0
description: Session reflection — surface digest at start, memory capture enforcement at stop
events:
  - SessionStart
  - Stop
  - PreCompact
metadata:
  name: name
  description: description
